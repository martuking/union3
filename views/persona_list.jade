extends layout

block content
  h1= title
  
  ul
  each persona in persona_list
    li 
      a(href=persona.url) #{persona.nombre} 
      |  (#{persona.apellido1})

  else
    li No hay personas en el listado